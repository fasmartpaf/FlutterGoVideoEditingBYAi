# Perception benchmark corpus

Ground-truth JSON per case lives here (also defined in `corpus.ts`).

Predictions and raw evidence: `tmp/perception-benchmark/runs/` (gitignored).

Do **not** invent timestamps for `RECORDING_REQUIRED` cases until media exists and is inspected.
