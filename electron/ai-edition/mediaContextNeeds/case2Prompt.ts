/**
 * Exact Case 2 FAST UI EVENT user prompt (benchmark / in-app routing failure).
 * Shared fixture for classifier + live routing regressions.
 */
export const CASE_2_EXACT_PROMPT = `Watch this complete recording carefully and explain what visibly happens from beginning to end.

Call out any brief UI changes, menus, popovers, notifications, or temporary states that appear, even if they are visible only for a short time.

Describe only what you can actually determine from the recording. Do not invent button names, menu names, clicks, or actions that are not clearly visible.

There is no need to discuss backend diagnostics or JSON. Explain it naturally as if you had watched the recording yourself.`;
